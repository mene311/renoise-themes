# Session 2026-04-19 - Renoise Themes

## Tasks Completed

### 1. Pixel Map Refinement
- Fixed text pixel mapping in `tools/build-maps.js` - text pixels were being set to UNMATCHED, causing fonts to default to white
- Added nearest-element search (radius 20px) to assign text pixels to their owning background element
- Coverage improved from ~73% → ~80%

### 2. Theme Toggle (Light/Dark)
- Added `?theme=light|dark` query param support
- Fixed header.ejs to pass theme variable to all templates
- Added cookie-parser dependency
- Theme icons: 🌙/☀️ in nav header

### 3. Slug URLs
- Added `slug` column to themes table
- Auto-generate slugs: "Deep Ocean" → "deep-ocean"
- Routes: `/theme/adwaita-dark-flat` instead of `/theme/1`
- Legacy ID redirect: `/theme/1` → `/theme/adwaita-dark-flat`
- Import/export `getThemeBySlug` in app.js

### 4. Palette Colors Reduced
- Reduced from 6 to 4 colors shown on theme cards

### 5. Persistent Server
- Installed pm2 locally for process management
- `npx pm2 start app.js --name renoise-themes`
- `npx pm2 restart renoise-themes`

### 6. Database Migration
- Added slug column via ALTER TABLE
- Populated existing theme slugs

## Issues Fixed
- getThemeBySlug not imported in app.js
- theme variable missing in upload.ejs render calls
- Preview images: data exists, URL path correct

## Commands
```bash
npm install pm2 --save-dev  # already done
npx pm2 start app.js --name renoise-themes
npx pm2 restart renoise-themes
npx pm2 logs renoise-themes

# Rebuild maps
node tools/build-maps.js pattern screenshot-A-pattern.png screenshot-B-pattern.png screenshot-C-pattern.png screenshot-D-pattern.png
node tools/build-maps.js mixer screenshot-A-mixer.png screenshot-B-mixer.png screenshot-C-mixer.png screenshot-D-mixer.png
node tools/build-maps.js waveform screenshot-A-waveform.png screenshot-B-waveform.png screenshot-C-waveform.png screenshot-D-waveform.png

# Regenerate previews
node tools/generate-all-previews.js
```

## Future
- Multi-tool hosting: job-search, builder, dashboards → separate routes in same repo